import Layout from '../components/shared/Layout';
import AuthForm from '../components/Auth/AuthForm';
const Auth = () => {
  return (
    <Layout>
      <AuthForm />
    </Layout>
  );
};

export default Auth;
